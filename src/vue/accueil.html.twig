{% extends 'base.html.twig' %}

{% block title %}
    SeikoCraft - Accueil
{% endblock %}

{% block content %}
<div class="pt-16">
    <!-- Hero Section -->
    <div class="relative">
        <section id="hero">
        <script src="./js/diapo.js"></script>
            <div class="absolute inset-0">
                <div class="absolute inset-0 bg-gray-900/70 mix-blend-multiply"></div>
            </div>
            
            <div class="relative max-w-7xl mx-auto py-24 px-4 sm:py-32 sm:px-6 lg:px-8">
                <h1 class="text-4xl font-extrabold text-white sm:text-5xl lg:text-6xl">
                    Montres SeikoMods
                </h1>

                <p class="mt-6 text-xl text-white max-w-3xl">
                    Découvrez notre collection exclusive de montres Seiko modifiées.
                    Chaque pièce est unique, assemblée à la main avec une attention particulière aux détails pour vous plaire.
                </p>
                <div class="mt-10">
                    <a href="index.php?page=categories" class="inline-flex items-center px-6 py-3 border border-transparent text-base font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700">
                        Découvrir la Collection
                    </a>
                </div>
            </div>
        </section>
    </div>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
        <h2 class="text-3xl font-extrabold text-gray-900 text-center mb-12">
            Nos SeikoMods tendances 
        </h2>
        
        <div class="relative">
            <!-- Podium Background -->
            <div class="absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t from-gray-100 to-transparent"></div>
            
            <div class="grid grid-cols-3 gap-8 relative">
                <!-- Podium Items -->

                <div class="transform translate-y-8 transition-all duration-300 hover:-translate-y-2">
                    <div class="bg-white rounded-lg shadow-lg overflow-hidden">
                        <div class="relative">
                            <img src="{{produit2.image_url}}" alt="Montre 1" class="w-full h-64 object-cover">
                            <div class="absolute top-4 left-4">
                                <div class="flex items-center justify-center w-8 h-8 rounded-full bg-indigo-600 text-white font-bold">
                                2
                                </div>
                            </div>
                        </div>
                        <div class="p-6">
                            <h3 class="text-xl font-semibold text-gray-900"><td>{{produit2.name}}</td></h3>
                            <p class="mt-2 text-gray-600 line-clamp-2">{{produit2.description}}</p>
                            <p class="mt-4 text-2xl font-bold text-indigo-600">{{produit2.price}} €</p>
                            <a href="#" class="mt-4 w-full inline-flex justify-center items-center px-6 py-3 border border-transparent text-base font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700">
                                Voir le détail
                            </a>
                        </div>
                    </div>
                </div>
                
                <div class="transform translate-y-0 transition-all duration-300 hover:-translate-y-2">
                    <div class="bg-white rounded-lg shadow-lg overflow-hidden">
                        <div class="relative">
                            <img src="{{produit1.image_url}}" alt="Montre 2" class="w-full h-64 object-cover">
                            <div class="absolute top-4 left-4">
                                <div class="flex items-center justify-center w-8 h-8 rounded-full bg-indigo-600 text-white font-bold">
                                    1
                                </div>
                            </div>
                        </div>
                        <div class="p-6">
                            <h3 class="text-xl font-semibold text-gray-900">{{produit1.name}}</h3>
                            <p class="mt-2 text-gray-600 line-clamp-2">{{produit1.description}}</p>
                            <p class="mt-4 text-2xl font-bold text-indigo-600">{{produit1.price}} €</p>
                            <a href="#" class="mt-4 w-full inline-flex justify-center items-center px-6 py-3 border border-transparent text-base font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700">
                                Voir le détail
                            </a>
                        </div>
                    </div>
                </div>
                
                <div class="transform translate-y-16 transition-all duration-300 hover:-translate-y-2">
                    <div class="bg-white rounded-lg shadow-lg overflow-hidden">
                        <div class="relative">
                            <img src="{{produit3.image_url}}" alt="Montre 3" class="w-full h-64 object-cover">
                            <div class="absolute top-4 left-4">
                                <div class="flex items-center justify-center w-8 h-8 rounded-full bg-indigo-600 text-white font-bold">
                                    3
                                </div>
                            </div>
                        </div>
                        <div class="p-6">
                            <h3 class="text-xl font-semibold text-gray-900">{{produit3.name}}</h3>
                            <p class="mt-2 text-gray-600 line-clamp-2">{{produit3.description}}</p>
                            <p class="mt-4 text-2xl font-bold text-indigo-600">{{produit3.price}} €</p>
                            <a href="#" class="mt-4 w-full inline-flex justify-center items-center px-6 py-3 border border-transparent text-base font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700">
                                Voir le détail
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}